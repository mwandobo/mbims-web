To add a new menu

Open Components then sidebar
Add the menu there following the structure
Open app folder
Create a folder for that feature




when edit is not populating just check below
use-crud-operator-hook
update below method
populateFormForEdit



when creating a button with confirmation use the following util
  showConfirmationModal
  use like below

      const handleSubmit = () => {
          showConfirmationModal({
              title: 'Are You Sure?',
              text: `Are You Sure You Want To Close Project: ${data?.name}?`,
              onConfirm: onSave,  // Action to perform on confirmation
              onCancel: () => console.log('User canceled the action'), // Optional cancel action
          });
      };